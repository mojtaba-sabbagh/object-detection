# ----- CPU-only PyTorch wheels -----
# This ensures pip can find the "+cpu" builds of torch/vision/audio.
#--extra-index-url https://download.pytorch.org/whl/cpu

# ----- Frameworks / server -----
Django>=4.2,<5.0
djangorestframework>=3.15,<3.16
django-cors-headers>=4.4,<5
waitress>=2.1,<3
python-dotenv>=1.0,<2

# ----- YOLO + imaging -----
ultralytics>=8.2,<9
opencv-python>=4.8,<5
Pillow>=10,<11
numpy>=1.26,<2.0

# ----- PyTorch (CPU-only) -----
# Pin to the CPU builds. The "+cpu" tag requires the extra index above.
torch==2.4.1+cpu
torchvision==0.19.1+cpu
torchaudio==2.4.1+cpu
